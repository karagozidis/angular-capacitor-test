import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ang.captest',
  appName: 'angular-capacitor-test',
  webDir: 'dist/angular-capacitor-test',
  bundledWebRuntime: false
};

export default config;
